interface:
  display_name: "Probot"
  short_description: "Repo-specific patterns and workflows for probot"
  default_prompt: "Use the probot repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true